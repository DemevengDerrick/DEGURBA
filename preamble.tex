% --- Content of preamble.tex (Revised 2) ---
\usepackage{graphicx}
\usepackage{fancyhdr}
% \usepackage{sectsty} % Using titlesec instead
\usepackage{titlesec}
\usepackage{lscape}
\usepackage{float}
\usepackage{xcolor}
\usepackage{titling}
% \usepackage{framed} % Removed
% \usepackage{color} % Removed
\usepackage{fancyvrb}
\usepackage{fvextra} % For Pandoc highlighting
\usepackage[most]{tcolorbox}

% Define the custom shaded box using tcolorbox
\newenvironment{UNFPAShadedBox}{%
\begin{tcolorbox}[sharp corners, enhanced, colback=white, boxrule=0pt, borderline west={2pt}{0pt}{orange}]%
}{\end{tcolorbox}}

% Define a custom highlighting environment based on the shaded box
% RENAMED from "Highlighting" to "UNFPAHighlightBox" to avoid conflict with Pandoc
\newenvironment{UNFPAHighlightBox}{\begin{UNFPAShadedBox}}{\end{UNFPAShadedBox}} % <<< RENAMED HERE

% UNFPA Orange color definition
\definecolor{unfpaorange}{RGB}{255,102,0}

% --- Section/Subsection Formatting using titlesec ---
\titleformat{\section}
  {\normalfont\Large\bfseries\color{unfpaorange}} {\thesection} {1em} {}
\titleformat{\subsection}
  {\normalfont\large\bfseries\color{unfpaorange}} {\thesubsection} {1em} {}

% --- Footer Configuration ---
\pagestyle{fancy}
\fancyhf{}
\rfoot{Derrick Demeveng - UNFPA}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

% --- Title Configuration ---
\pretitle{%
  \begin{center}%
  \IfFileExists{unfpa_logo.png}{%
    \includegraphics[width=4cm]{unfpa_logo.png}\par\vspace{1cm}%
  }{%
    \texttt{unfpa\_logo.png NOT FOUND}\par\vspace{1cm}%
  }
}
\posttitle{\end{center}}

% Make fancyhdr settings apply correctly
\pagestyle{fancy}
% --- End of preamble.tex ---